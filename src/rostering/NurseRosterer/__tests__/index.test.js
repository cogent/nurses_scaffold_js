import {NurseRosterer} from '../index'

it('builds a roster from available nurse list', () => {
  expect(NurseRosterer).toBeDefined()
  expect('You should write some tests for the NurseRosterer.').toHaveLength(50)
})
